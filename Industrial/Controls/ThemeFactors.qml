import QtQuick 2.6

QtObject {
    property real mainFontSize: 2.0     // Основной шрифт
    property real auxFontSize: 2.5      // Вспомогательный шрифт
    property real spacing: 2.5          // Отступы между компонентами
    property real margins: 4.0          // Отступы от внешних границ
    property real padding: 5.0          // Отступы от внутренних границ
    property real iconSize: 1.6         // Размер пиктограм
    property real scrollSize: 16        // Размер скроллбара
    property real sliderSize: 8.0       // Высота слайдера
    property real switchSize: 4.0       // Высота свича
    property real handleSize: 1.6       // Хендл у слайдеров и свичей
    property real checkmarkSize: 1.6    // Чекбоксы и радио
    property real floatSize: 0.8        // Плавающие кнопки
    property real rounding: 8           // Радиус скругления
    property real underline: 16         // Подчёркивание
    property real shadow: 10            // Размер тени
}
